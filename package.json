{
  "name": "website-mnr",
  "version": "1.0.0",
  "description": "A nimble static site for sports writing and data visualisation.",
  "license": "MIT",
  "type": "module",
  "private": true,
  "scripts": {
    "data": "node scripts/build-data.mjs",
    "brief": "node scripts/check-brief.mjs",
    "write-brief": "node scripts/write-brief.mjs",
    "build": "npm run data && npm run brief && eleventy",
    "serve": "npm run data && npm run brief && eleventy --serve",
    "clean": "rm -rf _site"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.1.6",
    "@anthropic-ai/sdk": "^0.123.0",
    "read-excel-file": "^9.3.10"
  }
}
